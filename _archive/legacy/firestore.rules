rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /content_items/{itemId} {
      allow read, write: if true; // À restreindre quand on ajoute l'auth
    }
  }
}
